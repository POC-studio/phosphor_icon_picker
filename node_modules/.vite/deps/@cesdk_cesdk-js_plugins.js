import {
  CreativeEditorSDK
} from "./chunk-CWM6ZUOS.js";
import {
  __publicField
} from "./chunk-4YJ6HKSX.js";

// node_modules/@cesdk/cesdk-js/plugins/index.js
var e = class {
  constructor(e2 = {}) {
    __publicField(this, "name", "cesdk-blur-asset-source");
    __publicField(this, "version", CreativeEditorSDK.version);
    __publicField(this, "addedAssetSourceIds", []);
    this.config = e2;
  }
  getAssetLibraryEntries(e2) {
    var _a;
    if ((_a = this.config.assetLibraryEntries) == null ? void 0 : _a[e2]) {
      const s = this.config.assetLibraryEntries[e2];
      return Array.isArray(s) ? s : [s];
    }
    return { "ly.img.blur": ["ly.img.blur"] }[e2] || [];
  }
  async initialize({ engine: e2, cesdk: s }) {
    const t2 = this.config.baseURL || (s == null ? void 0 : s.getBaseURL()) || e2.editor.getSetting("basePath");
    if (!t2) throw new Error("Cannot determine baseURL");
    const r2 = t2.replace(/\/*$/, "/"), i2 = new Set(e2.asset.findAllSources()), o2 = "ly.img.blur";
    if (!i2.has(o2)) {
      const s2 = `${r2}${o2}/content.json`;
      await e2.asset.addLocalAssetSourceFromJSONURI(s2, { matcher: this.config.include });
    }
    if (this.addedAssetSourceIds.push(o2), s) {
      s.onReset(() => this.cleanup({ engine: e2, cesdk: s }));
      const t3 = this.getAssetLibraryEntries(o2);
      for (const e3 of t3) s.ui.updateAssetLibraryEntry(e3, { sourceIds: ({ currentIds: e4 }) => [.../* @__PURE__ */ new Set([...e4, o2])] });
    }
  }
  cleanup({ engine: e2, cesdk: s }) {
    for (const t2 of this.addedAssetSourceIds) {
      if (s) {
        const e3 = this.getAssetLibraryEntries(t2);
        for (const r2 of e3) s.ui.updateAssetLibraryEntry(r2, { sourceIds: ({ currentIds: e4 }) => e4.filter((e5) => e5 !== t2) });
      }
      try {
        e2.asset.removeSource(t2);
      } catch (e3) {
        console.warn("Unable to remove source with id: ", t2);
      }
    }
    this.addedAssetSourceIds = [];
  }
};
var t = "ly.img.caption.presets";
var r = ["backgroundColor/enabled", "backgroundColor/color", "backgroundColor/cornerRadius", "backgroundColor/paddingBottom", "backgroundColor/paddingLeft", "backgroundColor/paddingRight", "backgroundColor/paddingTop", "blend/mode", "dropShadow/enabled", "dropShadow/color", "dropShadow/offset/x", "dropShadow/offset/y", "dropShadow/blurRadius/x", "dropShadow/blurRadius/y", "dropShadow/clip", "fill/enabled", "fill/solid/color", "opacity", "stroke/enabled", "stroke/color", "stroke/width", "stroke/style", "stroke/position", "stroke/cornerGeometry", "{{blockType}}/typeface", "{{blockType}}/fontSize", "{{blockType}}/horizontalAlignment", "{{blockType}}/verticalAlignment", "{{blockType}}/letterSpacing", "{{blockType}}/lineHeight", "{{blockType}}/automaticFontSizeEnabled", "{{blockType}}/maxAutomaticFontSize", "{{blockType}}/minAutomaticFontSize", "{{blockType}}/paragraphSpacing"];
function i(e2) {
  const s = e2.scene.getCurrentPage();
  if (null != s) return e2.block.getChildren(s).find((s2) => "//ly.img.ubq/captionTrack" === e2.block.getType(s2));
}
function o(e2) {
  const s = i(e2);
  if (null == s) return 1;
  try {
    const t2 = e2.block.getMetadata(s, "previousScale"), r2 = parseFloat(t2);
    return Number.isNaN(r2) ? 1 : r2;
  } catch (e3) {
    return 1;
  }
}
function n(e2, s, t2) {
  const r2 = o(e2), n2 = e2.block.getTextFontSizes(s)[0] / r2, a2 = Math.round(n2 * t2);
  if (e2.block.setTextFontSize(s, a2), e2.block.isStrokeEnabled(s)) {
    const i2 = e2.block.getStrokeWidth(s) / r2 * t2;
    e2.block.setStrokeWidth(s, i2);
  }
  if (e2.block.isDropShadowEnabled(s)) {
    const i2 = e2.block.getDropShadowOffsetX(s) / r2, o2 = e2.block.getDropShadowOffsetY(s) / r2, n3 = e2.block.getDropShadowBlurRadiusX(s) / r2, a3 = e2.block.getDropShadowBlurRadiusY(s) / r2;
    e2.block.setDropShadowOffsetX(s, i2 * t2), e2.block.setDropShadowOffsetY(s, o2 * t2), e2.block.setDropShadowBlurRadiusX(s, n3 * t2), e2.block.setDropShadowBlurRadiusY(s, a3 * t2);
  }
  !function(e3, s2) {
    const t3 = i(e3);
    null != t3 && e3.block.setMetadata(t3, "previousScale", s2.toString());
  }(e2, t2);
}
var a = class {
  constructor(e2, s, t2) {
    __publicField(this, "engine");
    __publicField(this, "assets", []);
    __publicField(this, "customFetchImpl");
    __publicField(this, "baseURL");
    this.engine = e2, this.baseURL = s, this.customFetchImpl = t2 ?? window.fetch.bind(window);
  }
  async initialize() {
    await this.fetchAssets(), this.createSource();
  }
  async fetchAssets() {
    const e2 = function(...e3) {
      return e3.map((e4, s) => 0 === s ? e4.replace(/\/$/, "") : e4.replace(/^\//, "").replace(/\/$/, "")).join("/");
    }(this.baseURL, `/${t}/content.json`);
    try {
      const s = await this.customFetchImpl(e2);
      if (!s.ok) throw new Error(s.statusText);
      const r2 = await s.json();
      if (void 0 === r2.assets) throw new Error(`Invalid content.json for assets: ${t}`);
      this.assets = r2.assets.map((e3) => (e3.meta && Object.entries(e3.meta).forEach(([s2, t2]) => {
        const r3 = t2.toString();
        if (r3.includes("{{base_url}}")) {
          const t3 = r3.replace("{{base_url}}", this.baseURL).replace(/([^:])\/\/+/g, "$1/");
          e3.meta && (e3.meta[s2] = t3);
        }
      }), e3));
    } catch (e3) {
      console.warn("Could not load caption presets:", e3), this.assets = [];
    }
  }
  createSource() {
    if (new Set(this.engine.asset.findAllSources()).has(t)) return;
    const e2 = { id: t, findAssets: (e3) => {
      const { page: s, perPage: r2, excludeGroups: n2, groups: a2, locale: c2 = "en", query: d2, sortActiveFirst: l, sortKey: u2, sortingOrder: g, tags: h2 } = e3, f = i(this.engine), p2 = null != f && this.engine.block.hasMetadata(f, "activePreset") ? this.engine.block.getMetadata(f, "activePreset") : void 0;
      let y = [...this.assets];
      if (a2 && a2.length > 0 && (y = y.filter((e4) => {
        var _a;
        return (_a = e4.groups) == null ? void 0 : _a.some((e5) => a2.includes(e5));
      })), d2 && "" !== d2.trim()) {
        const e4 = d2.toLowerCase().trim();
        y = y.filter((s2) => {
          var _a, _b;
          return (((_b = (_a = s2.label) == null ? void 0 : _a[c2]) == null ? void 0 : _b.toLowerCase()) || "").includes(e4);
        });
      }
      h2 && h2.length > 0 && (y = y.filter((e4) => {
        var _a;
        return (((_a = e4.tags) == null ? void 0 : _a[c2]) ?? []).some((e5) => h2.includes(e5));
      })), n2 && n2.length > 0 && (y = y.filter((e4) => !(e4.groups ?? []).some((e5) => n2.includes(e5)))), "label" === u2 && "None" !== g && y.sort((e4, s2) => {
        var _a, _b, _c, _d;
        const t2 = ((_b = (_a = e4.label) == null ? void 0 : _a[c2]) == null ? void 0 : _b.toLocaleLowerCase()) || "", r3 = ((_d = (_c = s2.label) == null ? void 0 : _c[c2]) == null ? void 0 : _d.toLocaleLowerCase()) || "";
        return t2 < r3 ? "Ascending" === g ? -1 : 1 : t2 > r3 ? "Ascending" === g ? 1 : -1 : 0;
      }), l && p2 && y.sort((e4, s2) => e4.id === p2 ? -1 : s2.id === p2 ? 1 : 0);
      const b2 = y.length, m = s * r2, A2 = m + r2, k = y.slice(m, A2).map((e4) => {
        var _a, _b, _c, _d;
        const s2 = (_b = (_a = e4.payload) == null ? void 0 : _a.properties) == null ? void 0 : _b.map((e5) => "caption/scale" === e5.property ? { ...e5, value: o(this.engine) } : e5);
        return { id: e4.id, label: (_c = e4.label) == null ? void 0 : _c[c2], active: e4.id === p2, context: { id: t }, meta: e4.meta, groups: e4.groups, tags: (_d = e4.tags) == null ? void 0 : _d[c2], payload: { ...e4.payload, properties: s2 }, locale: c2 };
      });
      return Promise.resolve({ assets: k, total: b2, currentPage: s });
    }, getGroups: () => {
      const e3 = /* @__PURE__ */ new Set();
      return this.assets.forEach((s) => {
        s.groups && s.groups.forEach((s2) => {
          e3.add(s2);
        });
      }), Promise.resolve(Array.from(e3));
    }, removeAsset: (e3) => {
      const s = this.assets.findIndex((s2) => s2.id === e3);
      -1 !== s && this.assets.splice(s, 1);
    }, addAsset: (e3) => {
      const s = this.assets.findIndex((s2) => s2.id === e3.id);
      -1 !== s ? this.assets[s] = e3 : this.assets.push(e3);
    }, applyAssetToBlock: (e3, s) => this.applyPreset(e3, s), applyAssetProperty: (e3, s) => this.applyProperty(e3, s), fetchAsset: async (e3, s) => new Promise((t2) => {
      var _a, _b, _c, _d;
      const r2 = this.assets.find((s2) => s2.id === e3);
      if (null == r2) t2(null);
      else {
        const e4 = (s == null ? void 0 : s.locale) ?? "en", n2 = i(this.engine), a2 = null != n2 && this.engine.block.hasMetadata(n2, "activePreset") ? this.engine.block.getMetadata(n2, "activePreset") : void 0, c2 = (_b = (_a = r2.payload) == null ? void 0 : _a.properties) == null ? void 0 : _b.map((e5) => "caption/scale" === e5.property ? { ...e5, value: o(this.engine) } : e5);
        t2({ id: r2.id, label: (_c = r2.label) == null ? void 0 : _c[e4], active: r2.id === a2, meta: r2.meta, groups: r2.groups, tags: (_d = r2.tags) == null ? void 0 : _d[e4], payload: { ...r2.payload, properties: c2 } });
      }
    }) };
    this.engine.asset.addSource(e2);
  }
  async applyPreset(e2, s) {
    var _a, _b, _c;
    const o2 = (_a = e2 == null ? void 0 : e2.meta) == null ? void 0 : _a.uri;
    if (null == o2) throw new Error(`The asset ${e2.id} does not have a preset block URL.`);
    const n2 = await this.customFetchImpl(o2);
    if (!n2.ok) throw new Error(`Failed to fetch the preset block from ${o2}`);
    const a2 = await n2.text(), [c2] = await this.engine.block.loadFromString(a2), d2 = null != c2 ? this.engine.block.getType(c2) : void 0;
    if ("//ly.img.ubq/text" !== d2 && "//ly.img.ubq/caption" !== d2) return;
    const l = i(this.engine), u2 = null != l ? this.engine.block.getChildren(l) : [];
    r.forEach((e3) => {
      const t2 = e3.replace("{{blockType}}", "//ly.img.ubq/text" === d2 ? "text" : "caption"), r2 = this.engine.block.getPropertyType(t2), i2 = e3.replace("{{blockType}}", "caption");
      switch (r2) {
        case "Int": {
          const e4 = this.engine.block.getInt(c2, t2);
          this.engine.block.setInt(s, i2, e4);
          break;
        }
        case "Float": {
          const e4 = this.engine.block.getFloat(c2, t2);
          this.engine.block.setFloat(s, i2, e4);
          break;
        }
        case "Double": {
          const e4 = this.engine.block.getDouble(c2, t2);
          this.engine.block.setDouble(s, i2, e4);
          break;
        }
        case "Bool": {
          const e4 = this.engine.block.getBool(c2, t2);
          this.engine.block.setBool(s, i2, e4);
          break;
        }
        case "Enum": {
          if ("stroke/position" === t2) break;
          const e4 = this.engine.block.getEnum(c2, t2);
          this.engine.block.setEnum(s, i2, e4);
          break;
        }
        case "Color": {
          if ("fill/solid/color" === t2) {
            const [e5] = this.engine.block.getTextColors(c2);
            this.engine.block.setColor(s, i2, e5), "Text" === this.engine.editor.getEditMode() && this.engine.editor.setEditMode("Transform"), this.engine.block.setTextColor(s, e5);
            break;
          }
          const e4 = this.engine.block.getColor(c2, t2);
          this.engine.block.setColor(s, i2, e4);
          break;
        }
        case "String": {
          if ("text/typeface" === t2 || "caption/typeface" === t2) {
            const e5 = this.engine.block.getTypeface(c2);
            this.engine.block.setTypeface(s, e5);
            break;
          }
          const e4 = this.engine.block.getString(c2, t2);
          this.engine.block.setString(s, i2, e4);
          break;
        }
      }
    }), (_c = (_b = e2.payload) == null ? void 0 : _b.properties) == null ? void 0 : _c.forEach(async (s2) => {
      "Color" === s2.type && await this.applyProperty(e2, s2);
    });
    const [g] = this.engine.block.getTextFontWeights(c2);
    null != g && this.engine.block.setTextFontWeight(s, g);
    const [h2] = this.engine.block.getTextFontStyles(c2);
    null != h2 && this.engine.block.setTextFontStyle(s, h2), function(e3, s2, t2) {
      const r2 = { in: e3.block.getInAnimation(s2), loop: e3.block.getLoopAnimation(s2), out: e3.block.getOutAnimation(s2) }, i2 = (s3, t3) => {
        const i3 = r2[t3], o3 = (() => {
          switch (t3) {
            case "in":
              return e3.block.getInAnimation(s3);
            case "loop":
              return e3.block.getLoopAnimation(s3);
            case "out":
              return e3.block.getOutAnimation(s3);
            default:
              return;
          }
        })();
        if (null != o3 && e3.block.isValid(o3) && e3.block.destroy(o3), e3.block.isValid(i3)) {
          const r3 = e3.block.duplicate(i3);
          switch (t3) {
            case "in":
              e3.block.setInAnimation(s3, r3);
              break;
            case "loop":
              e3.block.setLoopAnimation(s3, r3);
              break;
            case "out":
              e3.block.setOutAnimation(s3, r3);
          }
        }
      };
      t2.forEach((e4) => {
        i2(e4, "in"), i2(e4, "loop"), i2(e4, "out");
      });
    }(this.engine, c2, u2), null != l && this.engine.block.setMetadata(l, "activePreset", e2.id), this.engine.block.destroy(c2), this.engine.editor.addUndoStep(), this.engine.asset.assetSourceContentsChanged(t);
  }
  async applyProperty(e2, s) {
    const r2 = i(this.engine);
    if (null == r2) return;
    const [o2] = this.engine.block.findAllSelected(), a2 = "//ly.img.ubq/caption" === (null != o2 ? this.engine.block.getType(o2) : void 0) ? o2 : this.engine.block.getChildren(r2)[0];
    if (null != a2) {
      switch (s.type) {
        case "Color":
          if ("fill/solid/color" === s.property) {
            this.engine.block.setColor(a2, s.property, s.value), this.engine.block.setTextColor(a2, s.value);
            break;
          }
          this.engine.block.setColor(a2, s.property, s.value);
          break;
        case "Int":
          this.engine.block.setInt(a2, s.property, s.value);
          break;
        case "Float":
          if ("caption/scale" === s.property) {
            n(this.engine, a2, s.value);
            break;
          }
          this.engine.block.setFloat(a2, s.property, s.value);
          break;
        case "Double":
          this.engine.block.setDouble(a2, s.property, s.value);
          break;
        case "Boolean":
          this.engine.block.setBool(a2, s.property, s.value);
          break;
        case "Enum":
          this.engine.block.setEnum(a2, s.property, s.value);
          break;
        case "String":
          this.engine.block.setString(a2, s.property, s.value);
      }
      this.assets = function(e3, s2, t2) {
        return e3.map((e4) => {
          var _a, _b, _c, _d;
          if ("caption/scale" === t2.property) {
            const s3 = (_b = (_a = e4.payload) == null ? void 0 : _a.properties) == null ? void 0 : _b.map((e5) => "caption/scale" === e5.property ? { ...e5, value: t2.value } : e5);
            return { ...e4, payload: { ...e4.payload, properties: s3 } };
          }
          if (e4.id === s2.id) {
            const s3 = (_d = (_c = e4.payload) == null ? void 0 : _c.properties) == null ? void 0 : _d.map((e5) => e5.property === t2.property ? { ...e5, value: t2.value } : e5);
            return { ...e4, payload: { ...e4.payload, properties: s3 } };
          }
          return e4;
        });
      }(this.assets, e2, s), this.engine.asset.assetSourceContentsChanged(t);
    }
  }
};
var c = class {
  constructor(e2 = {}) {
    __publicField(this, "name", "cesdk-caption-presets-asset-source");
    __publicField(this, "version", CreativeEditorSDK.version);
    __publicField(this, "addedAssetSourceIds", []);
    this.config = e2;
  }
  getAssetLibraryEntries(e2) {
    var _a;
    if ((_a = this.config.assetLibraryEntries) == null ? void 0 : _a[e2]) {
      const s = this.config.assetLibraryEntries[e2];
      return Array.isArray(s) ? s : [s];
    }
    return { "ly.img.caption.presets": ["ly.img.library.captionPresets"] }[e2] || [];
  }
  async initialize({ engine: e2, cesdk: s }) {
    const t2 = this.config.baseURL || (s == null ? void 0 : s.getBaseURL()) || e2.editor.getSetting("basePath");
    if (!t2) throw new Error("Cannot determine baseURL");
    const r2 = t2.replace(/\/*$/, "/"), i2 = new a(e2, r2, this.config.customFetchImpl);
    await i2.initialize();
    const o2 = "ly.img.caption.presets";
    if (this.addedAssetSourceIds.push(o2), s) {
      s.onReset(() => this.cleanup({ engine: e2, cesdk: s }));
      const t3 = this.getAssetLibraryEntries(o2);
      for (const e3 of t3) s.ui.updateAssetLibraryEntry(e3, { sourceIds: ({ currentIds: e4 }) => [.../* @__PURE__ */ new Set([...e4, o2])] });
    }
  }
  cleanup({ engine: e2, cesdk: s }) {
    for (const t2 of this.addedAssetSourceIds) {
      if (s) {
        const e3 = this.getAssetLibraryEntries(t2);
        for (const r2 of e3) s.ui.updateAssetLibraryEntry(r2, { sourceIds: ({ currentIds: e4 }) => e4.filter((e5) => e5 !== t2) });
      }
      try {
        e2.asset.removeSource(t2);
      } catch (e3) {
        console.warn("Unable to remove source with id: ", t2);
      }
    }
    this.addedAssetSourceIds = [];
  }
};
var d = class {
  constructor(e2 = {}) {
    __publicField(this, "name", "cesdk-color-palette-asset-source");
    __publicField(this, "version", "1.61");
    __publicField(this, "addedAssetSourceIds", []);
    this.config = e2;
  }
  getAssetLibraryEntries(e2) {
    var _a;
    if ((_a = this.config.assetLibraryEntries) == null ? void 0 : _a[e2]) {
      const s = this.config.assetLibraryEntries[e2];
      return Array.isArray(s) ? s : [s];
    }
    return { "ly.img.color.palette": ["ly.img.colors"] }[e2] || [];
  }
  async initialize({ engine: e2, cesdk: s }) {
    const t2 = this.config.baseURL || (s == null ? void 0 : s.getBaseURL()) || e2.editor.getSetting("basePath");
    if (!t2) throw new Error("Cannot determine baseURL");
    const r2 = t2.replace(/\/*$/, "/"), i2 = new Set(e2.asset.findAllSources()), o2 = "ly.img.color.palette";
    if (!i2.has(o2)) {
      const s2 = `${r2}${o2}/content.json`;
      await e2.asset.addLocalAssetSourceFromJSONURI(s2, { matcher: this.config.include });
    }
    if (this.addedAssetSourceIds.push(o2), s) {
      s.onReset(() => this.cleanup({ engine: e2, cesdk: s }));
      const t3 = this.getAssetLibraryEntries(o2);
      for (const e3 of t3) s.ui.updateAssetLibraryEntry(e3, { sourceIds: ({ currentIds: e4 }) => [.../* @__PURE__ */ new Set([...e4, o2])] });
    }
  }
  cleanup({ engine: e2, cesdk: s }) {
    for (const t2 of this.addedAssetSourceIds) {
      if (s) {
        const e3 = this.getAssetLibraryEntries(t2);
        for (const r2 of e3) s.ui.updateAssetLibraryEntry(r2, { sourceIds: ({ currentIds: e4 }) => e4.filter((e5) => e5 !== t2) });
      }
      try {
        e2.asset.removeSource(t2);
      } catch (e3) {
        console.warn("Unable to remove source with id: ", t2);
      }
    }
    this.addedAssetSourceIds = [];
  }
};
var u = class {
  constructor(e2 = {}) {
    __publicField(this, "name", "cesdk-crop-presets-asset-source");
    __publicField(this, "version", CreativeEditorSDK.version);
    __publicField(this, "addedAssetSourceIds", []);
    this.config = e2;
  }
  getAssetLibraryEntries(e2) {
    var _a;
    if ((_a = this.config.assetLibraryEntries) == null ? void 0 : _a[e2]) {
      const s = this.config.assetLibraryEntries[e2];
      return Array.isArray(s) ? s : [s];
    }
    return { "ly.img.crop.presets": ["ly.img.cropPresets"] }[e2] || [];
  }
  async initialize({ engine: e2, cesdk: s }) {
    const t2 = this.config.baseURL || (s == null ? void 0 : s.getBaseURL()) || e2.editor.getSetting("basePath");
    if (!t2) throw new Error("Cannot determine baseURL");
    const r2 = t2.replace(/\/*$/, "/"), i2 = new Set(e2.asset.findAllSources()), o2 = "ly.img.crop.presets";
    if (!i2.has(o2)) {
      const s2 = `${r2}${o2}/content.json`;
      await e2.asset.addLocalAssetSourceFromJSONURI(s2, { matcher: this.config.include });
    }
    if (this.addedAssetSourceIds.push(o2), s) {
      s.onReset(() => this.cleanup({ engine: e2, cesdk: s }));
      const t3 = this.getAssetLibraryEntries(o2);
      for (const e3 of t3) s.ui.updateAssetLibraryEntry(e3, { sourceIds: ({ currentIds: e4 }) => [.../* @__PURE__ */ new Set([...e4, o2])] });
    }
  }
  cleanup({ engine: e2, cesdk: s }) {
    for (const t2 of this.addedAssetSourceIds) {
      if (s) {
        const e3 = this.getAssetLibraryEntries(t2);
        for (const r2 of e3) s.ui.updateAssetLibraryEntry(r2, { sourceIds: ({ currentIds: e4 }) => e4.filter((e5) => e5 !== t2) });
      }
      try {
        e2.asset.removeSource(t2);
      } catch (e3) {
        console.warn("Unable to remove source with id: ", t2);
      }
    }
    this.addedAssetSourceIds = [];
  }
};
var h = class {
  constructor(e2 = {}) {
    __publicField(this, "name", "cesdk-demo-asset-sources");
    __publicField(this, "version", CreativeEditorSDK.version);
    __publicField(this, "addedAssetSourceIds", []);
    this.config = e2;
  }
  globToRegex(e2) {
    const s = e2.replace(/[.+^${}()|[\]\\]/g, "\\$&").replace(/\*/g, ".*").replace(/\?/g, ".");
    return new RegExp(`^${s}$`, "i");
  }
  sourceMatchesAnyPattern(e2, s) {
    return s.some((s2) => {
      if (s2 === e2 || s2.startsWith(`${e2}.`)) return true;
      return this.globToRegex(s2).test(e2);
    });
  }
  filterAssetSources(e2, s) {
    return e2.filter((e3) => this.sourceMatchesAnyPattern(e3, s));
  }
  filterMatchersForSource(e2, s) {
    return s.filter((s2) => {
      if (s2 === e2 || s2.startsWith(`${e2}.`)) return true;
      return this.globToRegex(s2).test(e2);
    });
  }
  getAssetLibraryEntries(e2) {
    var _a;
    if ((_a = this.config.assetLibraryEntries) == null ? void 0 : _a[e2]) {
      const s = this.config.assetLibraryEntries[e2];
      return Array.isArray(s) ? s : [s];
    }
    return { "ly.img.image": ["ly.img.image"], "ly.img.video": ["ly.img.video"], "ly.img.audio": ["ly.img.audio"], "ly.img.templates": ["ly.img.templates"] }[e2] || [];
  }
  async initialize({ engine: e2, cesdk: s }) {
    const t2 = this.config.baseURL || (s == null ? void 0 : s.getBaseURL()) || e2.editor.getSetting("basePath");
    if (!t2) throw new Error("Cannot determine baseURL");
    const r2 = t2.replace(/\/*$/, "/"), i2 = new Set(e2.asset.findAllSources()), o2 = ["ly.img.audio", "ly.img.video", "ly.img.image", "ly.img.templates"], n2 = this.config.include ? this.filterAssetSources(o2, this.config.include) : o2;
    for (const s2 of n2) if (!i2.has(s2)) {
      const t3 = `${r2}${s2}/content.json`, i3 = this.config.include ? this.filterMatchersForSource(s2, this.config.include) : void 0;
      await e2.asset.addLocalAssetSourceFromJSONURI(t3, { matcher: i3 }), this.addedAssetSourceIds.push(s2);
    }
    if (s) {
      s.onReset(() => this.cleanup({ engine: e2, cesdk: s }));
      for (const e3 of this.addedAssetSourceIds) {
        const t3 = this.getAssetLibraryEntries(e3);
        for (const r3 of t3) s.ui.updateAssetLibraryEntry(r3, { sourceIds: ({ currentIds: s2 }) => [.../* @__PURE__ */ new Set([...s2, e3])] });
      }
    }
  }
  cleanup({ engine: e2, cesdk: s }) {
    for (const t2 of this.addedAssetSourceIds) {
      if (s) {
        const e3 = this.getAssetLibraryEntries(t2);
        for (const r2 of e3) s.ui.updateAssetLibraryEntry(r2, { sourceIds: ({ currentIds: e4 }) => e4.filter((e5) => e5 !== t2) });
      }
      try {
        e2.asset.removeSource(t2);
      } catch (e3) {
        console.warn("Unable to remove source with id: ", t2);
      }
    }
    this.addedAssetSourceIds = [];
  }
};
var p = class {
  constructor(e2 = {}) {
    __publicField(this, "name", "cesdk-effects-asset-source");
    __publicField(this, "version", CreativeEditorSDK.version);
    __publicField(this, "addedAssetSourceIds", []);
    this.config = e2;
  }
  getAssetLibraryEntries(e2) {
    var _a;
    if ((_a = this.config.assetLibraryEntries) == null ? void 0 : _a[e2]) {
      const s = this.config.assetLibraryEntries[e2];
      return Array.isArray(s) ? s : [s];
    }
    return { "ly.img.effect": ["ly.img.effect"] }[e2] || [];
  }
  async initialize({ engine: e2, cesdk: s }) {
    const t2 = this.config.baseURL || (s == null ? void 0 : s.getBaseURL()) || e2.editor.getSetting("basePath");
    if (!t2) throw new Error("Cannot determine baseURL");
    const r2 = t2.replace(/\/*$/, "/"), i2 = new Set(e2.asset.findAllSources()), o2 = "ly.img.effect";
    if (!i2.has(o2)) {
      const s2 = `${r2}${o2}/content.json`;
      await e2.asset.addLocalAssetSourceFromJSONURI(s2, { matcher: this.config.include });
    }
    if (this.addedAssetSourceIds.push(o2), s) {
      s.onReset(() => this.cleanup({ engine: e2, cesdk: s }));
      const t3 = this.getAssetLibraryEntries(o2);
      for (const e3 of t3) s.ui.updateAssetLibraryEntry(e3, { sourceIds: ({ currentIds: e4 }) => [.../* @__PURE__ */ new Set([...e4, o2])] });
    }
  }
  cleanup({ engine: e2, cesdk: s }) {
    for (const t2 of this.addedAssetSourceIds) {
      if (s) {
        const e3 = this.getAssetLibraryEntries(t2);
        for (const r2 of e3) s.ui.updateAssetLibraryEntry(r2, { sourceIds: ({ currentIds: e4 }) => e4.filter((e5) => e5 !== t2) });
      }
      try {
        e2.asset.removeSource(t2);
      } catch (e3) {
        console.warn("Unable to remove source with id: ", t2);
      }
    }
    this.addedAssetSourceIds = [];
  }
};
var b = class {
  constructor(e2 = {}) {
    __publicField(this, "name", "cesdk-filters-asset-source");
    __publicField(this, "version", CreativeEditorSDK.version);
    __publicField(this, "addedAssetSourceIds", []);
    this.config = e2;
  }
  getAssetLibraryEntries(e2) {
    var _a;
    if ((_a = this.config.assetLibraryEntries) == null ? void 0 : _a[e2]) {
      const s = this.config.assetLibraryEntries[e2];
      return Array.isArray(s) ? s : [s];
    }
    return { "ly.img.filter": ["ly.img.filter"] }[e2] || [];
  }
  async initialize({ engine: e2, cesdk: s }) {
    const t2 = this.config.baseURL || (s == null ? void 0 : s.getBaseURL()) || e2.editor.getSetting("basePath");
    if (!t2) throw new Error("Cannot determine baseURL");
    const r2 = t2.replace(/\/*$/, "/"), i2 = new Set(e2.asset.findAllSources()), o2 = "ly.img.filter";
    if (!i2.has(o2)) {
      const s2 = `${r2}${o2}/content.json`;
      await e2.asset.addLocalAssetSourceFromJSONURI(s2, { matcher: this.config.include });
    }
    if (this.addedAssetSourceIds.push(o2), s) {
      s.onReset(() => this.cleanup({ engine: e2, cesdk: s }));
      const t3 = this.getAssetLibraryEntries(o2);
      for (const e3 of t3) s.ui.updateAssetLibraryEntry(e3, { sourceIds: ({ currentIds: e4 }) => [.../* @__PURE__ */ new Set([...e4, o2])] });
    }
  }
  cleanup({ engine: e2, cesdk: s }) {
    for (const t2 of this.addedAssetSourceIds) {
      if (s) {
        const e3 = this.getAssetLibraryEntries(t2);
        for (const r2 of e3) s.ui.updateAssetLibraryEntry(r2, { sourceIds: ({ currentIds: e4 }) => e4.filter((e5) => e5 !== t2) });
      }
      try {
        e2.asset.removeSource(t2);
      } catch (e3) {
        console.warn("Unable to remove source with id: ", t2);
      }
    }
    this.addedAssetSourceIds = [];
  }
};
var A = class {
  constructor(e2 = {}) {
    __publicField(this, "name", "cesdk-page-presets-asset-source");
    __publicField(this, "version", CreativeEditorSDK.version);
    __publicField(this, "addedAssetSourceIds", []);
    this.config = e2;
  }
  getAssetLibraryEntries(e2) {
    var _a;
    if ((_a = this.config.assetLibraryEntries) == null ? void 0 : _a[e2]) {
      const s = this.config.assetLibraryEntries[e2];
      return Array.isArray(s) ? s : [s];
    }
    return { "ly.img.page.presets": ["ly.img.pagePresets"] }[e2] || [];
  }
  async initialize({ engine: e2, cesdk: s }) {
    const t2 = this.config.baseURL || (s == null ? void 0 : s.getBaseURL()) || e2.editor.getSetting("basePath");
    if (!t2) throw new Error("Cannot determine baseURL");
    const r2 = t2.replace(/\/*$/, "/"), i2 = new Set(e2.asset.findAllSources()), o2 = "ly.img.page.presets";
    if (!i2.has(o2)) {
      const s2 = `${r2}${o2}/content.json`;
      await e2.asset.addLocalAssetSourceFromJSONURI(s2, { matcher: this.config.include });
    }
    if (this.addedAssetSourceIds.push(o2), s) {
      s.onReset(() => this.cleanup({ engine: e2, cesdk: s }));
      const t3 = this.getAssetLibraryEntries(o2);
      for (const e3 of t3) s.ui.updateAssetLibraryEntry(e3, { sourceIds: ({ currentIds: e4 }) => [.../* @__PURE__ */ new Set([...e4, o2])] });
    }
  }
  cleanup({ engine: e2, cesdk: s }) {
    for (const t2 of this.addedAssetSourceIds) {
      if (s) {
        const e3 = this.getAssetLibraryEntries(t2);
        for (const r2 of e3) s.ui.updateAssetLibraryEntry(r2, { sourceIds: ({ currentIds: e4 }) => e4.filter((e5) => e5 !== t2) });
      }
      try {
        e2.asset.removeSource(t2);
      } catch (e3) {
        console.warn("Unable to remove source with id: ", t2);
      }
    }
    this.addedAssetSourceIds = [];
  }
};
var S = "ly.img.templates.premium";
var L = class {
  constructor(e2 = {}) {
    __publicField(this, "name", "cesdk-premium-asset-sources");
    __publicField(this, "version", CreativeEditorSDK.version);
    __publicField(this, "sourceAdded", false);
    this.config = e2;
  }
  globToRegex(e2) {
    const s = e2.replace(/[.+^${}()|[\]\\]/g, "\\$&").replace(/\*/g, ".*").replace(/\?/g, ".");
    return new RegExp(`^${s}$`, "i");
  }
  filterMatchersForSource(e2) {
    return e2.filter((e3) => {
      if (e3 === S || e3.startsWith(`${S}.`)) return true;
      return this.globToRegex(e3).test(S);
    });
  }
  getAssetLibraryEntries() {
    var _a;
    if ((_a = this.config.assetLibraryEntries) == null ? void 0 : _a[S]) {
      const e2 = this.config.assetLibraryEntries[S];
      return Array.isArray(e2) ? e2 : [e2];
    }
    return ["ly.img.templates"];
  }
  async initialize({ engine: e2, cesdk: s }) {
    const t2 = this.config.baseURL || (s == null ? void 0 : s.getBaseURL()) || e2.editor.getSetting("basePath");
    if (!t2) throw new Error("Cannot determine baseURL");
    const r2 = t2.replace(/\/*$/, "/");
    if (!new Set(e2.asset.findAllSources()).has(S)) {
      e2.asset.addLocalSource(S, void 0, async (s3) => {
        var _a;
        if ((_a = s3.meta) == null ? void 0 : _a.uri) return await e2.scene.loadFromArchiveURL(s3.meta.uri), e2.scene.get() ?? void 0;
      });
      const s2 = `${r2}${S}/content.json`, t3 = this.config.include ? this.filterMatchersForSource(this.config.include) : void 0;
      await e2.asset.addLocalAssetSourceFromJSONURI(s2, { matcher: t3 }), this.sourceAdded = true;
    }
    if (s && (s.onReset(() => this.cleanup({ engine: e2, cesdk: s })), this.sourceAdded)) {
      const e3 = this.getAssetLibraryEntries();
      for (const t3 of e3) s.ui.updateAssetLibraryEntry(t3, { sourceIds: ({ currentIds: e4 }) => [.../* @__PURE__ */ new Set([...e4, S])] });
    }
  }
  cleanup({ engine: e2, cesdk: s }) {
    if (this.sourceAdded) {
      if (s) {
        const e3 = this.getAssetLibraryEntries();
        for (const t2 of e3) s.ui.updateAssetLibraryEntry(t2, { sourceIds: ({ currentIds: e4 }) => e4.filter((e5) => e5 !== S) });
      }
      try {
        e2.asset.removeSource(S);
      } catch (e3) {
        console.warn("Unable to remove source with id: ", S);
      }
      this.sourceAdded = false;
    }
  }
};
var E = class {
  constructor(e2 = {}) {
    __publicField(this, "name", "cesdk-sticker-asset-source");
    __publicField(this, "version", CreativeEditorSDK.version);
    __publicField(this, "addedAssetSourceIds", []);
    this.config = e2;
  }
  getAssetLibraryEntries(e2) {
    var _a;
    if ((_a = this.config.assetLibraryEntries) == null ? void 0 : _a[e2]) {
      const s = this.config.assetLibraryEntries[e2];
      return Array.isArray(s) ? s : [s];
    }
    return { "ly.img.sticker": ["ly.img.sticker"] }[e2] || [];
  }
  async initialize({ engine: e2, cesdk: s }) {
    const t2 = this.config.baseURL || (s == null ? void 0 : s.getBaseURL()) || e2.editor.getSetting("basePath");
    if (!t2) throw new Error("Cannot determine baseURL");
    const r2 = t2.replace(/\/*$/, "/"), i2 = new Set(e2.asset.findAllSources()), o2 = "ly.img.sticker";
    if (!i2.has(o2)) {
      const s2 = `${r2}${o2}/content.json`;
      await e2.asset.addLocalAssetSourceFromJSONURI(s2, { matcher: this.config.include });
    }
    if (this.addedAssetSourceIds.push(o2), s) {
      s.onReset(() => this.cleanup({ engine: e2, cesdk: s }));
      const t3 = this.getAssetLibraryEntries(o2);
      for (const e3 of t3) s.ui.updateAssetLibraryEntry(e3, { sourceIds: ({ currentIds: e4 }) => [.../* @__PURE__ */ new Set([...e4, o2])] });
    }
  }
  cleanup({ engine: e2, cesdk: s }) {
    for (const t2 of this.addedAssetSourceIds) {
      if (s) {
        const e3 = this.getAssetLibraryEntries(t2);
        for (const r2 of e3) s.ui.updateAssetLibraryEntry(r2, { sourceIds: ({ currentIds: e4 }) => e4.filter((e5) => e5 !== t2) });
      }
      try {
        e2.asset.removeSource(t2);
      } catch (e3) {
        console.warn("Unable to remove source with id: ", t2);
      }
    }
    this.addedAssetSourceIds = [];
  }
};
var v = class {
  constructor(e2 = {}) {
    __publicField(this, "name", "cesdk-text-asset-source");
    __publicField(this, "version", CreativeEditorSDK.version);
    __publicField(this, "addedAssetSourceIds", []);
    this.config = e2;
  }
  getAssetLibraryEntries(e2) {
    var _a;
    if ((_a = this.config.assetLibraryEntries) == null ? void 0 : _a[e2]) {
      const s = this.config.assetLibraryEntries[e2];
      return Array.isArray(s) ? s : [s];
    }
    return { "ly.img.text": ["ly.img.text"] }[e2] || [];
  }
  async initialize({ engine: e2, cesdk: s }) {
    const t2 = this.config.baseURL || (s == null ? void 0 : s.getBaseURL()) || e2.editor.getSetting("basePath");
    if (!t2) throw new Error("Cannot determine baseURL");
    const r2 = t2.replace(/\/*$/, "/"), i2 = new Set(e2.asset.findAllSources()), o2 = "ly.img.text";
    if (!i2.has(o2)) {
      const s2 = `${r2}${o2}/content.json`;
      await e2.asset.addLocalAssetSourceFromJSONURI(s2, { matcher: this.config.include });
    }
    if (this.addedAssetSourceIds.push(o2), s) {
      s.onReset(() => this.cleanup({ engine: e2, cesdk: s }));
      const t3 = this.getAssetLibraryEntries(o2);
      for (const e3 of t3) s.ui.updateAssetLibraryEntry(e3, { sourceIds: ({ currentIds: e4 }) => [.../* @__PURE__ */ new Set([...e4, o2])] });
    }
  }
  cleanup({ engine: e2, cesdk: s }) {
    for (const t2 of this.addedAssetSourceIds) {
      if (s) {
        const e3 = this.getAssetLibraryEntries(t2);
        for (const r2 of e3) s.ui.updateAssetLibraryEntry(r2, { sourceIds: ({ currentIds: e4 }) => e4.filter((e5) => e5 !== t2) });
      }
      try {
        e2.asset.removeSource(t2);
      } catch (e3) {
        console.warn("Unable to remove source with id: ", t2);
      }
    }
    this.addedAssetSourceIds = [];
  }
};
var U = class {
  constructor(e2 = {}) {
    __publicField(this, "name", "cesdk-text-component-asset-source");
    __publicField(this, "version", CreativeEditorSDK.version);
    __publicField(this, "addedAssetSourceIds", []);
    this.config = e2;
  }
  getAssetLibraryEntries(e2) {
    var _a;
    if ((_a = this.config.assetLibraryEntries) == null ? void 0 : _a[e2]) {
      const s = this.config.assetLibraryEntries[e2];
      return Array.isArray(s) ? s : [s];
    }
    return { "ly.img.text.components": ["ly.img.text"] }[e2] || [];
  }
  async initialize({ engine: e2, cesdk: s }) {
    const t2 = this.config.baseURL || (s == null ? void 0 : s.getBaseURL()) || e2.editor.getSetting("basePath");
    if (!t2) throw new Error("Cannot determine baseURL");
    const r2 = t2.replace(/\/*$/, "/"), i2 = new Set(e2.asset.findAllSources()), o2 = "ly.img.text.components";
    if (!i2.has(o2)) {
      const s2 = `${r2}${o2}/content.json`;
      await e2.asset.addLocalAssetSourceFromJSONURI(s2, { matcher: this.config.include });
    }
    if (this.addedAssetSourceIds.push(o2), s) {
      s.onReset(() => this.cleanup({ engine: e2, cesdk: s }));
      const t3 = this.getAssetLibraryEntries(o2);
      for (const e3 of t3) s.ui.updateAssetLibraryEntry(e3, { sourceIds: ({ currentIds: e4 }) => [.../* @__PURE__ */ new Set([...e4, o2])] });
    }
  }
  cleanup({ engine: e2, cesdk: s }) {
    for (const t2 of this.addedAssetSourceIds) {
      if (s) {
        const e3 = this.getAssetLibraryEntries(t2);
        for (const r2 of e3) s.ui.updateAssetLibraryEntry(r2, { sourceIds: ({ currentIds: e4 }) => e4.filter((e5) => e5 !== t2) });
      }
      try {
        e2.asset.removeSource(t2);
      } catch (e3) {
        console.warn("Unable to remove source with id: ", t2);
      }
    }
    this.addedAssetSourceIds = [];
  }
};
var C = class {
  constructor(e2 = {}) {
    __publicField(this, "name", "cesdk-typeface-asset-source");
    __publicField(this, "version", CreativeEditorSDK.version);
    __publicField(this, "addedAssetSourceIds", []);
    this.config = e2;
  }
  getAssetLibraryEntries(e2) {
    var _a;
    if ((_a = this.config.assetLibraryEntries) == null ? void 0 : _a[e2]) {
      const s = this.config.assetLibraryEntries[e2];
      return Array.isArray(s) ? s : [s];
    }
    return { "ly.img.typeface": ["ly.img.typefaces"] }[e2] || [];
  }
  async initialize({ engine: e2, cesdk: s }) {
    const t2 = this.config.baseURL || (s == null ? void 0 : s.getBaseURL()) || e2.editor.getSetting("basePath");
    if (!t2) throw new Error("Cannot determine baseURL");
    const r2 = t2.replace(/\/*$/, "/"), i2 = new Set(e2.asset.findAllSources()), o2 = "ly.img.typeface";
    if (!i2.has(o2)) {
      const s2 = `${r2}${o2}/content.json`;
      await e2.asset.addLocalAssetSourceFromJSONURI(s2, { matcher: this.config.include });
    }
    if (this.addedAssetSourceIds.push(o2), s) {
      s.onReset(() => this.cleanup({ engine: e2, cesdk: s }));
      const t3 = this.getAssetLibraryEntries(o2);
      for (const e3 of t3) s.ui.updateAssetLibraryEntry(e3, { sourceIds: ({ currentIds: e4 }) => [.../* @__PURE__ */ new Set([...e4, o2])] });
    }
  }
  cleanup({ engine: e2, cesdk: s }) {
    for (const t2 of this.addedAssetSourceIds) {
      if (s) {
        const e3 = this.getAssetLibraryEntries(t2);
        for (const r2 of e3) s.ui.updateAssetLibraryEntry(r2, { sourceIds: ({ currentIds: e4 }) => e4.filter((e5) => e5 !== t2) });
      }
      try {
        e2.asset.removeSource(t2);
      } catch (e3) {
        console.warn("Unable to remove source with id: ", t2);
      }
    }
    this.addedAssetSourceIds = [];
  }
};
var T = [{ id: "ly.img.image.upload", mimeTypes: ["image/jpeg", "image/png", "image/webp", "image/svg+xml", "image/bmp", "image/gif"], assetLibraryEntries: ["ly.img.image", "ly.img.upload"] }, { id: "ly.img.video.upload", mimeTypes: ["application/json", "video/mp4", "video/quicktime", "video/webm", "video/matroska", "image/gif"], assetLibraryEntries: ["ly.img.video", "ly.img.upload"] }, { id: "ly.img.audio.upload", mimeTypes: ["audio/mpeg", "audio/mp3", "audio/x-m4a", "audio/wav"], assetLibraryEntries: ["ly.img.audio", "ly.img.upload"] }];
var F = class {
  constructor(e2 = {}) {
    __publicField(this, "name", "cesdk-upload-asset-sources");
    __publicField(this, "version", CreativeEditorSDK.version);
    __publicField(this, "addedAssetSourceIds", []);
    __publicField(this, "addedLibraryEntryMappings", /* @__PURE__ */ new Map());
    this.config = e2;
  }
  isExplicitMode() {
    var _a;
    return ((_a = this.config.include) == null ? void 0 : _a.some((e2) => "string" == typeof e2)) ?? false;
  }
  getExplicitIncludes() {
    return this.config.include ? new Set(this.config.include.filter((e2) => "string" == typeof e2)) : /* @__PURE__ */ new Set();
  }
  getCustomizations() {
    return this.config.include ? this.config.include.filter((e2) => "object" == typeof e2) : [];
  }
  getAssetLibraryEntries(e2) {
    return e2 ? Array.isArray(e2) ? e2 : [e2] : [];
  }
  async initialize({ engine: e2, cesdk: s }) {
    const t2 = new Set(e2.asset.findAllSources()), r2 = this.isExplicitMode(), i2 = this.getExplicitIncludes(), o2 = this.getCustomizations(), n2 = /* @__PURE__ */ new Map();
    if (r2) for (const e3 of T) i2.has(e3.id) && n2.set(e3.id, e3);
    else for (const e3 of T) n2.set(e3.id, e3);
    for (const e3 of o2) {
      const s2 = n2.get(e3.id);
      s2 ? n2.set(e3.id, { ...s2, ...e3 }) : n2.set(e3.id, e3);
    }
    const a2 = Array.from(n2.values());
    for (const r3 of a2) {
      const { id: i3, mimeTypes: o3, assetLibraryEntries: n3 } = r3;
      if (!t2.has(i3) && (e2.asset.addLocalSource(i3, o3), this.addedAssetSourceIds.push(i3), s)) {
        const e3 = this.getAssetLibraryEntries(n3);
        this.addedLibraryEntryMappings.set(i3, e3);
        for (const t3 of e3) s.ui.updateAssetLibraryEntry(t3, { sourceIds: ({ currentIds: e4 }) => [.../* @__PURE__ */ new Set([...e4, i3])] });
      }
    }
    s && s.onReset(() => this.cleanup({ engine: e2, cesdk: s }));
  }
  cleanup({ engine: e2, cesdk: s }) {
    for (const t2 of this.addedAssetSourceIds) {
      if (s) {
        const e3 = this.addedLibraryEntryMappings.get(t2) ?? [];
        for (const r2 of e3) s.ui.updateAssetLibraryEntry(r2, { sourceIds: ({ currentIds: e4 }) => e4.filter((e5) => e5 !== t2) });
      }
      try {
        e2.asset.removeSource(t2);
      } catch (e3) {
        console.warn("Unable to remove source with id: ", t2);
      }
    }
    this.addedAssetSourceIds = [], this.addedLibraryEntryMappings.clear();
  }
};
var j = class {
  constructor(e2 = {}) {
    __publicField(this, "name", "cesdk-vectorshape-asset-source");
    __publicField(this, "version", CreativeEditorSDK.version);
    __publicField(this, "addedAssetSourceIds", []);
    this.config = e2;
  }
  getAssetLibraryEntries(e2) {
    var _a;
    if ((_a = this.config.assetLibraryEntries) == null ? void 0 : _a[e2]) {
      const s = this.config.assetLibraryEntries[e2];
      return Array.isArray(s) ? s : [s];
    }
    return { "ly.img.vector.shape": ["ly.img.vector.shape", "ly.img.shape.replace"] }[e2] || [];
  }
  async initialize({ engine: e2, cesdk: s }) {
    const t2 = this.config.baseURL || (s == null ? void 0 : s.getBaseURL()) || e2.editor.getSetting("basePath");
    if (!t2) throw new Error("Cannot determine baseURL");
    const r2 = t2.replace(/\/*$/, "/"), i2 = new Set(e2.asset.findAllSources()), o2 = "ly.img.vector.shape";
    if (!i2.has(o2)) {
      const s2 = `${r2}${o2}/content.json`;
      await e2.asset.addLocalAssetSourceFromJSONURI(s2, { matcher: this.config.include });
    }
    if (this.addedAssetSourceIds.push(o2), s) {
      s.onReset(() => this.cleanup({ engine: e2, cesdk: s }));
      const t3 = this.getAssetLibraryEntries(o2);
      for (const e3 of t3) s.ui.updateAssetLibraryEntry(e3, { sourceIds: ({ currentIds: e4 }) => [.../* @__PURE__ */ new Set([...e4, o2])] });
    }
  }
  cleanup({ engine: e2, cesdk: s }) {
    for (const t2 of this.addedAssetSourceIds) {
      if (s) {
        const e3 = this.getAssetLibraryEntries(t2);
        for (const r2 of e3) s.ui.updateAssetLibraryEntry(r2, { sourceIds: ({ currentIds: e4 }) => e4.filter((e5) => e5 !== t2) });
      }
      try {
        e2.asset.removeSource(t2);
      } catch (e3) {
        console.warn("Unable to remove source with id: ", t2);
      }
    }
    this.addedAssetSourceIds = [];
  }
};
export {
  e as BlurAssetSource,
  c as CaptionPresetsAssetSource,
  d as ColorPaletteAssetSource,
  u as CropPresetsAssetSource,
  h as DemoAssetSources,
  p as EffectsAssetSource,
  b as FiltersAssetSource,
  A as PagePresetsAssetSource,
  L as PremiumTemplatesAssetSource,
  E as StickerAssetSource,
  v as TextAssetSource,
  U as TextComponentAssetSource,
  C as TypefaceAssetSource,
  F as UploadAssetSources,
  j as VectorShapeAssetSource
};
//# sourceMappingURL=@cesdk_cesdk-js_plugins.js.map
